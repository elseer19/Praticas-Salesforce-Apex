// Simular uma mensagem de boas-vindas para um usuário em um sistema.

Boolean usuarioLogado = false;

if (usuarioLogado){
    System.debug('Bem-vindo(a) de volta!');
} else {
    System.debug('Por favor, faça o login para continuar.');
}
