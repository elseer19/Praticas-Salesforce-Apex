// Verificar se um produto em um e-commerce está disponível para venda

Integer quantidadeEmEstoque = 0;

if (quantidadeEmEstoque > 0 ){
    System.debug('Produto disponível para compra!');
} else {
    System.debug('Produto esgotado no momento.');
}
